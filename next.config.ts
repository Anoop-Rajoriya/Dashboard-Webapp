import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // for using this ensure to install the next-canary version.
  // experimental: {
  //   ppr: "incremental",
  // },
};

export default nextConfig;
